import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ReviewInputComponent = () => {
  return (
    <View>
      <Text>ReviewInputComponent</Text>
    </View>
  );
};

export default ReviewInputComponent;

const styles = StyleSheet.create({});
