import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ReviewComponent = () => {
  return (
    <View>
      <Text>ReviewComponent</Text>
    </View>
  );
};

export default ReviewComponent;

const styles = StyleSheet.create({});
